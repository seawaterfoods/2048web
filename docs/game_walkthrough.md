# 2048 Game Walkthrough

## 專案概述 (Overview)
已成功實作基於瀏覽器的 2048 小遊戲。本專案從零開始，使用現代化前端技術棧構建，包含完整且經過測試的遊戲核心邏輯、流暢的動畫效果與響應式介面。**目前已完成第四階段的語系與版面優化。**

## 實作功能 (Features)

### 核心功能 (Core Features)
1.  **完整遊戲邏輯**: 2048 移動與合併規則，分數計算，勝負判斷。
2.  **響應式介面 (RWD)**: 支援桌機與行動裝置佈局，具備版面對齊優化。
3.  **現代化 UI**: 4x4 網格，方塊顏色區分，流暢的 Slide/Pop/Appear 動畫。

### 擴充功能 (Extensions - Phase 2, 3 & 4)
1.  **行動裝置支援 (Mobile Support)**: 
    *   實作觸控手勢 (Swipe Gestures: 上/下/左/右 滑動)。
    *   防止遊戲區域的預設滾動行為與下拉重整 (Viewport Lock)。
2.  **狀態持久化 (Persistence)**:
    *   自動儲存遊戲進度 (Auto-save) 至 LocalStorage。
    *   即使關閉瀏覽器，重新開啟後仍可接續遊玩。
3.  **視覺與動畫優化 (Advanced Visuals)**:
    *   **Framer Motion**: 使用彈簧物理效果取代 CSS 過渡。
    *   **粒子特效**: 合併方塊時產生動態噴濺效果。
    *   **背景對齊**: 解決了在各種螢幕縮放比例下的方塊對齊偏移。
4.  **智慧自動操作 (Auto Play)**:
    *   採用 Expectimax 演算法的 AI，支援自動遊玩。
5.  **主題與語系切換**:
    *   支援深色/淺色模式 (Dark/Light Mode)。
    *   支援 **繁體中文、英文、日文** 多語系。
6.  **PWA 支援與手機安裝**:
    *   支援離線執行與安裝至手機主畫面。
7.  **多元操控方式 (Multi-modal Controls)**:
    *   **鍵盤控制**：支援方向鍵與 WASD。
    *   **觸控手勢**：行動裝置上可直接滑動。
    *   **畫面按鈕**：新增了可點擊的「上下左右」方向鍵，具備實體回饋感，方便滑鼠操作或習慣按鈕的玩家。
    *   **懸浮模式 (Floating Mode)**：新增「🚀」切換按鈕。點擊後可將按鈕組懸浮於右下角，避免在高度較小的螢幕上被遮擋。

## 畫面控制預覽 (Control Pad Preview)
![畫面控制按鍵](/Users/joewu/.gemini/antigravity/brain/b140cba3-f0c2-4557-a895-79d9287143bd/game_with_arrow_buttons_1769670011709.png)

## 如何安裝到手機 (Mobile Installation)
1.  **環境準備**: 電腦端執行 `npm run preview:host`。
2.  **連線**: 手機與電腦連至同 Wi-Fi，開啟顯示的 `Network` 網址。
3.  **安裝**: 
    *   **iOS (Safari)**: 「分享」->「加入主畫面」。
    *   **Android (Chrome)**: 「...」->「安裝應用程式」。


## 專案結構 (Project Structure)
```
src/
├── components/         # UI Elements (Board, Tile, Particle, Layout)
├── hooks/              # Logic Hooks (useGameLogic, useAutoPlay, useTouchGestures)
├── utils/              # Algorithms & Utils (grid, tile, ai)
├── types/              # TS Definitions
└── styles/             # Global SCSS & Theme Variables
```

## 如何執行 (How to Run)

### 1. 安裝依賴
```bash
npm install
```

### 2. 啟動開發伺服器
```bash
npm run dev
```
瀏覽器打開 `http://localhost:5173` 即可遊玩。支援手機瀏覽器訪問測試觸控功能。

### 3. 執行測試
```bash
npm test
```
驗證核心邏輯正確性。

### 4. 建置生產版本 (PWA & Desktop Ready)
```bash
npm run build
```
建置結果位於 `dist/` 目錄。

### 5. 建置桌面應用程式 (macOS / Windows)
```bash
npm run electron:build
```
打包後的執行檔將位於 `release/` 目錄下。
建議使用 `npm run preview` 預覽生產版本以測試 PWA 功能。

