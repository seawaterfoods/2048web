# 🚀 GitHub Pages 部署指南

您的專案已經設定好可以部署到 GitHub Pages！

## 網址
部署後您的遊戲會在：
**https://seawaterfoods.github.io/2048web/**

## 📋 已完成的設定

✅ 安裝 `gh-pages` 套件
✅ 加入部署指令到 `package.json`
✅ 設定 Vite base path 為 `/2048web/`
✅ 建立 GitHub Actions 自動部署工作流程
✅ 重新建置專案

## 🎯 部署方式（二選一）

### 方法一：手動部署（推薦新手）

執行一個指令即可：
```bash
npm run deploy
```

這會自動：
1. 建置專案
2. 推送到 `gh-pages` 分支
3. 完成！

### 方法二：自動部署（推薦）

已設定 GitHub Actions，每次推送程式碼到 main 分支時自動部署。

**首次啟用步驟：**
1. 前往 GitHub 儲存庫：https://github.com/seawaterfoods/2048web
2. 點擊 **Settings** → **Pages**
3. Source 選擇 **GitHub Actions**
4. 儲存

之後每次 `git push` 就會自動部署！

## 🔧 首次部署步驟

### 使用手動部署
```bash
# 1. 執行部署
npm run deploy

# 2. 前往 GitHub 設定
# https://github.com/seawaterfoods/2048web/settings/pages

# 3. Source 選擇 "gh-pages" 分支

# 4. 等待 1-2 分鐘

# 5. 訪問您的網站
# https://seawaterfoods.github.io/2048web/
```

### 使用自動部署
```bash
# 1. 提交並推送新的設定檔
git add .
git commit -m "Add GitHub Pages deployment"
git push origin main

# 2. 前往 GitHub 設定
# https://github.com/seawaterfoods/2048web/settings/pages

# 3. Source 選擇 "GitHub Actions"

# 4. 等待 Actions 執行完成（約 1-2 分鐘）

# 5. 訪問您的網站
# https://seawaterfoods.github.io/2048web/
```

## 📝 更新網站

### 手動部署
```bash
# 修改程式碼後
npm run deploy
```

### 自動部署
```bash
# 修改程式碼後
git add .
git commit -m "Update game"
git push origin main
# 自動部署！
```

## 🎨 自訂網域（選用）

如果您有自己的網域：

1. 在 `public/` 資料夾建立 `CNAME` 檔案
2. 內容填入您的網域，例如：`2048.yourdomain.com`
3. 在網域 DNS 設定中加入 CNAME 記錄指向 `seawaterfoods.github.io`
4. 重新部署

## ✅ 驗證部署

部署完成後，檢查：
- [ ] 網站可以正常開啟
- [ ] 遊戲功能正常
- [ ] 懸浮與拖曳功能正常
- [ ] PWA 安裝功能正常
- [ ] 多語言切換正常

## 🐛 疑難排解

### 問題：404 錯誤
**解決**：確認 GitHub Pages 設定中的 Source 是正確的分支

### 問題：CSS/JS 載入失敗
**解決**：檢查 `vite.config.ts` 中的 `base: '/2048web/'` 設定

### 問題：部署後是舊版本
**解決**：
1. 清除瀏覽器快取（Ctrl+Shift+R 或 Cmd+Shift+R）
2. 等待幾分鐘讓 GitHub Pages 更新

### 問題：GitHub Actions 失敗
**解決**：
1. 檢查 Actions 頁面的錯誤訊息
2. 確認 `package.json` 中的依賴都正確安裝

## 📊 監控部署狀態

- **手動部署**：終端機會顯示進度
- **自動部署**：前往 https://github.com/seawaterfoods/2048web/actions 查看

## 🎉 完成！

現在您的 2048 遊戲已經可以在網路上玩了！
分享連結給朋友：**https://seawaterfoods.github.io/2048web/**
